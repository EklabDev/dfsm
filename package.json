{
  "name": "dfsm",
  "version": "0.2.0",
  "private": true,
  "description": "Durable finite state machines with provider-agnostic persistence and queue-driven execution",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test:unit": "turbo run test:unit",
    "test:integration": "turbo run test:integration",
    "lint": "turbo run lint",
    "docs:api": "typedoc --entryPoints packages/core/src/index.ts --out docs/api",
    "publish:npm": "pnpm --filter @eklabdev/dfsm run build && pnpm --filter @eklabdev/dfsm publish --access public",
    "publish:npm:viz": "pnpm --filter @eklabdev/dfsm-viz run build && pnpm --filter @eklabdev/dfsm-viz publish --access public"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typedoc": "^0.26.0",
    "typescript": "^5.4.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
